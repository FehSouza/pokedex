import styled from 'styled-components'

export const PokemonList = styled.ul`
  margin-top: 15px;
  overflow-y: visible;
  z-index: 1;
  padding: 40px;
  padding-top: 0;
  overflow-x: hidden;
  height: fit-content;
`
